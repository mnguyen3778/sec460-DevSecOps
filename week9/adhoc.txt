ansible linux -a "uname -a"
ansible linux -a "uptime"
ansible linux -a "df -h"
ansible linux -a "free -m"
ansible linux -a "cat /etc/os-release"
ansible linux -a "hostname"
ansible linux -a "date"
ansible linux -a "who"
ansible linux -a "ps -ef | head"
ansible linux -a "ls /"
